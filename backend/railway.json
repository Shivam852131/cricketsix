{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install",
    "startCommand": "node server.js",
    "nixpacksConfig": {
      "pkgs": ["nodejs", "npm"],
      "cmd": "node server.js"
    }
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}